# 使用流程

Distill 采用「导入 → 选区 → 识别 → 导出」的四步极简工作流，极大地降低用户操作门槛。

![Distill 工作流主界面](/images/distill_app_studio_ui.jpg)

---

## 四步工作流

```
1. 导入视频 ──► 2. 8点手柄框选 ROI ──► 3. 选择离线/云端模式开始提取 ──► 4. 预览与导出
```

### 1. 导入视频
点击顶部工具栏的“导入视频”按钮或直接将视频文件拖拽至视频播放器区域。支持 MP4, MKV, AVI, MOV, WebM 等文件。

### 2. 8点手柄框选 ROI
在左侧视频预览图层上，使用 8 点手柄交互式调整、拉伸和拖拽框选硬字幕的真实区域。

### 3. 选择模式与开始提取
在顶部选择“离线模式”或“云端 API”模式。若使用云端 API 模式，需在设置中配置 API Key。点击“开始提取字幕”，系统将自动调度抽帧与识别。

### 4. 预览与导出
在右侧面板实时查看字幕时间轴列表，支持内联编辑文本、调整时间码、删除非字幕项，并一键导出为 SRT、VTT 或 TXT 格式。
